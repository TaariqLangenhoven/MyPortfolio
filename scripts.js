//cant use addEventListener on a anchor tag thats why we use onclick instead
const toggleMobileMenu = ()=> {
    const menu = document.getElementById("menu")
    menu.classList.toggle("active")
} 

